[project]
name = "swarm-discussion-runtime"
version = "0.0.0"
description = "Runtime incubator for the next swarm-discussion architecture"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["runtime"]
